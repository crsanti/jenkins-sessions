library identifier: 'jenkins-sessions-library@main', retriever: modernSMC([$class: 'GitSMCSource', remote: 'https://github.com/crsanti/jenkins-sessions-library.git'])

pipeline {
  agent any

  stages {
    stage('Audit tools') {
      steps {
        auditTools()
      }
    }
  }
}
